// spacing.ts
// Define the type for spacing values used in the application.

export type Spacing = {
    xs: number; // Extra small spacing
    sm: number; // Small spacing
    md: number; // Medium spacing
    lg: number; // Large spacing
    xl: number; // Extra large spacing
};
