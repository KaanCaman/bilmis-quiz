// radii.ts
// Define the type for border-radius values used in the application.

export type Radii = {
    none: number; // No rounding
    sm: number; // Small rounding
    md: number; // Medium rounding
    lg: number; // Large rounding
    full: number; // Fully rounded (circle shape)
};
