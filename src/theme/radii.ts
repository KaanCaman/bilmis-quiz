// Radii.ts
// Define border-radius values for creating consistent rounded corners.
// These values can be used to style components uniformly across the app.

import { Radii } from '../types/theme';

export const radii:Radii = {
    none: 0,     // No rounding
    sm: 4,       // Small rounding
    md: 8,       // Medium rounding
    lg: 16,      // Large rounding
    full: 9999,  // Fully rounded (circle shape)
  };
